{
  "compilerOptions": {
    /* Language and Environment */
    "target": "ES6",                                     /* Specify ECMAScript target version: "ES3" (default), "ES5", "ES6", "ES2015", "ES7", "ES2016", "ES2017", "ES2018", "ES2019", "ES2020", or "ESNEXT". */
    "lib": ["dom", "esnext"],                            /* Specify library files to be included in the compilation. */
    "jsx": "react-jsx",                                  /* Specify JSX code generation: "react" (default), "react-jsx", "react-jsxdom", or "react-native". */
    "moduleResolution": "node",                           /* Specify module resolution strategy: "node" (default), "classic", or "node16". */
    "esModuleInterop": true,                             /* Enables emit interoperability between CommonJS and ES Modules via creation of namespace objects for all imported files. */
    "forceConsistentCasingInFileNames": true ,                          /* Enable JSX support. */
    "noImplicitAny": false,                              /* Disable warnings for expressions and declarations with an implied 'any' type. */

    /* JavaScript Support */
    "allowJs": true,                                     /* Allow JavaScript files to be a part of the project. */
    "checkJs": true,                                     /* Enable type-checking for JavaScript files. */

    /* Type Checking */
    "strict": true,                                      /* Enable all strict type-checking options. */

    /* Completeness */
    "skipLibCheck": true                                 /* Skip type checking all .d.ts files. */
  },
  "include": ["src/**/*"],                                /* Include all files in the src directory. */
  "exclude": ["node_modules", "dist"]                    /* Exclude unnecessary directories. */
}
